/*
 * Created on 4 May 2015 ( Time 15:47:17 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.app.micimpact.rest.controller;

import java.util.HashMap;
import java.util.List;

import javax.annotation.Resource;

import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.bind.annotation.ResponseBody;
import org.springframework.web.bind.annotation.ResponseStatus;

import com.app.micimpact.bean.Postings;
import com.app.micimpact.business.service.PostingsService;
import com.app.micimpact.rest.common.AbstractRestController;

/**
 * Spring MVC controller for 'Postings' management.
 */

@RequestMapping("/notices")
@Controller
public class PostingsRestController extends AbstractRestController {

	@Resource
	private PostingsService postingsService;
	
	
	@RequestMapping( value="",
			method = {RequestMethod.GET, RequestMethod.POST},
			produces = MediaType.APPLICATION_JSON_VALUE)
    @ResponseStatus(HttpStatus.OK)
	@ResponseBody
	public HashMap<String, Object> findAll(@RequestParam(value="last_id", required=false) Integer lastId,
			@RequestParam(value="count", defaultValue="5") Integer count) 
	{
		HashMap<String, Object> ret = new HashMap<String, Object> ();
		
		if (lastId==null)
			ret.put("notices", postingsService.findByCount(count));
		else
			ret.put("notices", postingsService.findByLastId(lastId, count));
		
		return SUCCESS(ret);

	}

	
}
